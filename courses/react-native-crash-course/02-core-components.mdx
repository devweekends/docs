---
title: "02. Core Components"
description: "Master View, Text, Image, Button, TextInput, and all essential React Native components"
---

<Frame>
  <img src="/images/courses/react-native-crash-course/core-components.svg" alt="React Native Core Components" />
</Frame>

# Core Components

React Native provides a set of ready-to-use native components that translate to platform-specific equivalents. Instead of `<div>`, `<span>`, and `<img>`, you'll use `<View>`, `<Text>`, and `<Image>`.

If web development is like writing a letter in English, React Native is like writing instructions that get simultaneously translated into Japanese (iOS) and Korean (Android). The result in each language looks and feels natural to native speakers, because the underlying components are the real native ones -- not web views wearing a disguise.

## Component Mapping

| Web Component | React Native | iOS Native | Android Native |
|--------------|--------------|------------|----------------|
| `<div>` | `<View>` | `UIView` | `android.view.View` |
| `<span>`, `<p>` | `<Text>` | `UILabel` | `TextView` |
| `<img>` | `<Image>` | `UIImageView` | `ImageView` |
| `<input>` | `<TextInput>` | `UITextField` | `EditText` |
| `<button>` | `<Button>`, `<Pressable>` | `UIButton` | `Button` |
| `<ul>`, `<ol>` | `<FlatList>`, `<SectionList>` | `UITableView` | `RecyclerView` |

<Note>
**Key Concept**: React Native components compile to **native platform components**, not web views. This is why apps feel native!
</Note>

---

## View Component

`<View>` is the most fundamental component—it's a container that supports layout, styling, touch handling, and accessibility.

```tsx
import { View, StyleSheet } from 'react-native';

const App = () => {
  return (
    <View style={styles.container}>
      <View style={styles.box}>
        {/* Nested views */}
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
  box: {
    width: 100,
    height: 100,
    backgroundColor: '#007AFF',
  },
});
```

### View Props

```tsx
<View
  // Styling
  style={styles.container}
  
  // Touch handling
  onTouchStart={(e) => console.log('Touch started')}
  onTouchEnd={(e) => console.log('Touch ended')}
  
  // Accessibility
  accessible={true}
  accessibilityLabel="Main container"
  
  // Pointer events
  pointerEvents="box-none" // 'box-none' | 'none' | 'box-only' | 'auto'
>
  {/* Children */}
</View>
```

---

## SafeAreaView

`<SafeAreaView>` renders content within the safe area boundaries of a device (avoiding notches, home indicators, status bars, and rounded corners). Without it, your content could render behind the iPhone notch/Dynamic Island or the Android status bar.

```tsx
import { SafeAreaView, Text, StyleSheet } from 'react-native';

const App = () => {
  return (
    <SafeAreaView style={styles.container}>
      <Text>This text avoids the notch!</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
});
```

<Warning>
**iOS Only**: The built-in `SafeAreaView` from React Native only works on iOS 11+. On Android, it renders as a plain `View` with no safe area padding -- your content will still overlap the status bar. For proper cross-platform safe area handling, use the `react-native-safe-area-context` library instead (shown below). This is a common gotcha that catches every beginner.
</Warning>

### Better Alternative: react-native-safe-area-context

```bash
npm install react-native-safe-area-context
```

```tsx
import { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context';

const App = () => {
  return (
    <SafeAreaProvider>
      <SafeAreaView style={{ flex: 1 }}>
        <Text>Works on iOS and Android!</Text>
      </SafeAreaView>
    </SafeAreaProvider>
  );
};
```

---

## Text Component

All text **must** be wrapped in a `<Text>` component. Unlike the web, you cannot put text directly in a `<View>`. This trips up every web developer on day one -- if you write `<View>Hello</View>`, you will get a runtime error. The reason: native platforms have distinct text rendering systems (`UILabel` on iOS, `TextView` on Android), and React Native needs to know when content is text so it can route it to the right native component.

```tsx
import { Text, StyleSheet } from 'react-native';

const App = () => {
  return (
    <>
      <Text style={styles.title}>This is a title</Text>
      <Text style={styles.body}>This is body text</Text>
      
      {/* Nested text inherits parent styles */}
      <Text style={styles.bold}>
        This is <Text style={styles.italic}>bold with italic</Text>
      </Text>
    </>
  );
};

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#000',
  },
  body: {
    fontSize: 16,
    color: '#666',
  },
  bold: {
    fontWeight: 'bold',
  },
  italic: {
    fontStyle: 'italic',
  },
});
```

### Text Props

```tsx
<Text
  // Styling
  style={styles.text}
  
  // Number of lines
  numberOfLines={2}
  
  // Ellipsis mode
  ellipsizeMode="tail" // 'head' | 'middle' | 'tail' | 'clip'
  
  // Selection
  selectable={true}
  
  // Press handling
  onPress={() => console.log('Text pressed')}
  onLongPress={() => console.log('Long press')}
  
  // Accessibility
  accessibilityRole="header"
>
  Press me!
</Text>
```

### Text Truncation

```tsx
const LongText = () => {
  const longText = "This is a very long text that will be truncated to fit within the container...";
  
  return (
    <View style={{ width: 200 }}>
      <Text numberOfLines={2} ellipsizeMode="tail">
        {longText}
      </Text>
    </View>
  );
};
```

---

## Image Component

Display images from various sources: local files, network URLs, or base64 data.

### Local Images

```tsx
import { Image, StyleSheet } from 'react-native';

const App = () => {
  return (
    <Image
      source={require('./assets/logo.png')}
      style={styles.image}
    />
  );
};

const styles = StyleSheet.create({
  image: {
    width: 100,
    height: 100,
  },
});
```

<Note>
**Important**: Local images require explicit width and height. React Native cannot infer dimensions from `require()` the way a browser can progressively load and measure an `<img>` tag. This is a fundamental difference from web: every image needs dimensions upfront. For network images, if you do not set dimensions, the image will render at 0x0 pixels and be invisible.
</Note>

### Network Images

```tsx
const App = () => {
  return (
    <Image
      source={{ uri: 'https://example.com/image.png' }}
      style={{ width: 200, height: 200 }}
    />
  );
};
```

### Image with Loading States

```tsx
import { useState } from 'react';
import { Image, ActivityIndicator, View } from 'react-native';

const NetworkImage = ({ uri }) => {
  const [loading, setLoading] = useState(true);
  
  return (
    <View style={{ width: 200, height: 200 }}>
      {loading && (
        <ActivityIndicator
          style={{ position: 'absolute', alignSelf: 'center', top: '50%' }}
        />
      )}
      <Image
        source={{ uri }}
        style={{ width: '100%', height: '100%' }}
        onLoadStart={() => setLoading(true)}
        onLoadEnd={() => setLoading(false)}
        onError={() => setLoading(false)}
      />
    </View>
  );
};
```

### Image Props

```tsx
<Image
  source={{ uri: 'https://...' }}
  style={{ width: 200, height: 200 }}
  
  // Resize mode
  resizeMode="cover" // 'cover' | 'contain' | 'stretch' | 'repeat' | 'center'
  
  // Loading events
  onLoadStart={() => console.log('Loading started')}
  onLoad={(event) => console.log('Image loaded', event.nativeEvent)}
  onLoadEnd={() => console.log('Loading ended')}
  onError={(error) => console.error('Error loading image', error)}
  
  // Progress (iOS only)
  onProgress={(event) => {
    const { loaded, total } = event.nativeEvent;
    console.log(`Progress: ${(loaded / total) * 100}%`);
  }}
/>
```

### ImageBackground

For images as backgrounds:

```tsx
import { ImageBackground, Text, StyleSheet } from 'react-native';

const App = () => {
  return (
    <ImageBackground
      source={require('./assets/background.png')}
      style={styles.background}
      resizeMode="cover"
    >
      <Text style={styles.text}>Text over image</Text>
    </ImageBackground>
  );
};

const styles = StyleSheet.create({
  background: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    color: 'white',
    fontSize: 24,
    fontWeight: 'bold',
  },
});
```

---

## Button Component

Simple, platform-specific button.

```tsx
import { Button, Alert } from 'react-native';

const App = () => {
  return (
    <Button
      title="Press Me"
      onPress={() => Alert.alert('Button pressed!')}
      // IMPORTANT platform difference: on iOS, `color` sets the text color.
      // On Android, `color` sets the background color of the button.
      // This single prop behaves completely differently per platform.
      color="#007AFF"
      disabled={false}
    />
  );
};
```

<Warning>
**Limited Customization**: The built-in `<Button>` component has very limited styling options -- you cannot change its padding, border radius, font size, or add icons. It also looks completely different on iOS (a text link) versus Android (a Material Design button). For any real-world app, use `<Pressable>` or `<TouchableOpacity>` instead. The built-in Button is mainly useful for quick prototyping.
</Warning>

---

## Pressable Component (Recommended)

Modern, flexible pressable component with advanced press handling. `Pressable` was introduced in React Native 0.63 to replace the older `Touchable*` family. It gives you fine-grained control over every phase of a press interaction (press in, press out, long press) and lets you change styles dynamically based on the pressed state.

```tsx
import { Pressable, Text, StyleSheet } from 'react-native';

const CustomButton = () => {
  return (
    <Pressable
      style={({ pressed }) => [
        styles.button,
        pressed && styles.pressed,
      ]}
      onPress={() => console.log('Pressed')}
      onLongPress={() => console.log('Long pressed')}
      onPressIn={() => console.log('Press in')}
      onPressOut={() => console.log('Press out')}
      hitSlop={10} // Increase touch area
      pressRetentionOffset={10} // Keep active when finger moves
    >
      {({ pressed }) => (
        <Text style={styles.text}>
          {pressed ? 'Pressed!' : 'Press me'}
        </Text>
      )}
    </Pressable>
  );
};

const styles = StyleSheet.create({
  button: {
    padding: 15,
    backgroundColor: '#007AFF',
    borderRadius: 8,
    alignItems: 'center',
  },
  pressed: {
    backgroundColor: '#0051D5',
  },
  text: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
});
```

### Pressable with Ripple Effect (Android)

Android users expect a ripple effect on touch -- it is part of the Material Design language. On iOS, the convention is an opacity fade or scale change instead. Using `android_ripple` lets you provide the platform-native feedback Android users expect, while your `style` function handles iOS feedback.

```tsx
<Pressable
  android_ripple={{
    color: 'rgba(0, 0, 0, 0.1)', // Ripple color overlay
    borderless: false,             // Set true for circular ripple (e.g., icon buttons)
  }}
  style={({ pressed }) => [
    styles.button,
    // On iOS, provide opacity feedback since android_ripple is ignored
    Platform.OS === 'ios' && pressed && { opacity: 0.7 },
  ]}
>
  <Text>Cross-Platform Touch Feedback</Text>
</Pressable>
```

---

## TouchableOpacity (Legacy, Still Popular)

Fades the view when pressed.

```tsx
import { TouchableOpacity, Text, StyleSheet } from 'react-native';

const App = () => {
  return (
    <TouchableOpacity
      style={styles.button}
      onPress={() => console.log('Pressed')}
      activeOpacity={0.7}
    >
      <Text style={styles.text}>Press Me</Text>
    </TouchableOpacity>
  );
};

const styles = StyleSheet.create({
  button: {
    padding: 15,
    backgroundColor: '#007AFF',
    borderRadius: 8,
    alignItems: 'center',
  },
  text: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
});
```

---

## TextInput Component

For user text input.

```tsx
import { useState } from 'react';
import { TextInput, View, Text, StyleSheet } from 'react-native';

const App = () => {
  const [text, setText] = useState('');
  
  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        value={text}
        onChangeText={setText}
        placeholder="Enter text"
        placeholderTextColor="#999"
      />
      <Text>You typed: {text}</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 8,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 10,
  },
});
```

### TextInput Types

```tsx
// Email
<TextInput
  keyboardType="email-address"
  autoCapitalize="none"
  autoCorrect={false}
  textContentType="emailAddress" // iOS autocomplete
/>

// Password
<TextInput
  secureTextEntry={true}
  textContentType="password"
/>

// Phone number
<TextInput
  keyboardType="phone-pad"
  textContentType="telephoneNumber"
/>

// Number
<TextInput
  keyboardType="numeric"
/>

// Multiline
<TextInput
  multiline={true}
  numberOfLines={4}
  // textAlignVertical is Android-only. On iOS, multiline text starts at the
  // top by default. On Android, it centers vertically unless you set this.
  style={{ height: 100, textAlignVertical: 'top' }}
/>
```

### TextInput Props

```tsx
<TextInput
  // Value
  value={text}
  onChangeText={setText}
  
  // Placeholder
  placeholder="Enter text"
  placeholderTextColor="#999"
  
  // Keyboard
  keyboardType="default" // 'numeric' | 'email-address' | 'phone-pad' etc
  returnKeyType="done" // 'done' | 'go' | 'next' | 'search' | 'send'
  
  // Capitalization
  autoCapitalize="sentences" // 'none' | 'sentences' | 'words' | 'characters'
  
  // Autocorrect
  autoCorrect={true}
  
  // Security
  secureTextEntry={false}
  
  // Multiline
  multiline={false}
  numberOfLines={1}
  
  // Focus
  autoFocus={false}
  onFocus={() => console.log('Focused')}
  onBlur={() => console.log('Blurred')}
  
  // Submit
  onSubmitEditing={() => console.log('Submitted')}
  
  // Selection
  selection={{ start: 0, end: 0 }}
  onSelectionChange={(e) => console.log(e.nativeEvent.selection)}
/>
```

---

## ScrollView Component

For scrollable content.

```tsx
import { ScrollView, Text, View, StyleSheet } from 'react-native';

const App = () => {
  return (
    <ScrollView
      style={styles.container}
      showsVerticalScrollIndicator={true}
    >
      {[...Array(20)].map((_, i) => (
        <View key={i} style={styles.item}>
          <Text>Item {i + 1}</Text>
        </View>
      ))}
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  item: {
    padding: 20,
    borderBottomWidth: 1,
    borderBottomColor: '#ddd',
  },
});
```

### Horizontal ScrollView

```tsx
<ScrollView
  horizontal={true}
  showsHorizontalScrollIndicator={false}
  pagingEnabled={true} // Snap to pages
>
  {images.map((img, i) => (
    <Image key={i} source={img} style={{ width: 300, height: 200 }} />
  ))}
</ScrollView>
```

<Warning>
**Performance**: Do not use `ScrollView` for long lists. `ScrollView` renders **all** of its children at once, even those offscreen. If you put 1,000 items in a ScrollView, all 1,000 are rendered into memory immediately, causing slow load times and potential crashes on memory-constrained devices. Use `FlatList` instead -- it only renders items visible on screen (plus a small buffer), recycling views as you scroll. This is similar to how `RecyclerView` works on Android and `UITableView` works on iOS.
</Warning>

---

## ActivityIndicator

Loading spinner.

```tsx
import { ActivityIndicator, View } from 'react-native';

const App = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <ActivityIndicator size="large" color="#007AFF" />
    </View>
  );
};
```

---

## Switch Component

Toggle switch.

```tsx
import { useState } from 'react';
import { Switch, View, Text } from 'react-native';

const App = () => {
  const [isEnabled, setIsEnabled] = useState(false);
  
  return (
    <View style={{ flexDirection: 'row', alignItems: 'center', padding: 20 }}>
      <Text style={{ flex: 1 }}>Enable notifications</Text>
      <Switch
        value={isEnabled}
        onValueChange={setIsEnabled}
        trackColor={{ false: '#767577', true: '#81b0ff' }}
        thumbColor={isEnabled ? '#007AFF' : '#f4f3f4'}
      />
    </View>
  );
};
```

---

## Modal Component

Overlay content.

```tsx
import { useState } from 'react';
import { Modal, View, Text, Button, StyleSheet } from 'react-native';

const App = () => {
  const [visible, setVisible] = useState(false);
  
  return (
    <View style={styles.container}>
      <Button title="Show Modal" onPress={() => setVisible(true)} />
      
      <Modal
        visible={visible}
        animationType="slide" // 'none' | 'slide' | 'fade'
        transparent={true}
        onRequestClose={() => setVisible(false)}
      >
        <View style={styles.modalOverlay}>
          <View style={styles.modalContent}>
            <Text style={styles.modalText}>This is a modal!</Text>
            <Button title="Close" onPress={() => setVisible(false)} />
          </View>
        </View>
      </Modal>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  modalOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  modalContent: {
    backgroundColor: '#fff',
    padding: 20,
    borderRadius: 10,
    width: '80%',
  },
  modalText: {
    fontSize: 18,
    marginBottom: 15,
    textAlign: 'center',
  },
});
```

---

## Platform-Specific Code

### Using Platform.OS

```tsx
import { Platform, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
    ...Platform.select({
      ios: {
        backgroundColor: '#f0f0f0',
      },
      android: {
        backgroundColor: '#fff',
      },
      default: {
        backgroundColor: '#fff',
      },
    }),
  },
});
```

### Platform-Specific Files

```
MyComponent.ios.tsx   // iOS version
MyComponent.android.tsx // Android version
```

React Native's Metro bundler automatically picks the right file based on the build target. This is powerful for components where the iOS and Android implementations need to diverge significantly -- for example, a date picker that uses `UIDatePicker` on iOS but a Material Design picker on Android. Keep in mind that both files must export the same interface (same props, same component name) so the consuming code does not need to know which version it is using.

```tsx
// MyComponent.ios.tsx
export const MyComponent = () => <Text>iOS Version</Text>;

// MyComponent.android.tsx
export const MyComponent = () => <Text>Android Version</Text>;

// App.tsx
import { MyComponent } from './MyComponent'; // Auto-selects!
```

---

## Hands-On: Product Card Component

Let's build a reusable product card:

```tsx
import React from 'react';
import {
  View,
  Text,
  Image,
  TouchableOpacity,
  StyleSheet,
  Platform,
} from 'react-native';

interface ProductCardProps {
  title: string;
  price: number;
  image: string;
  onPress: () => void;
}

export const ProductCard: React.FC<ProductCardProps> = ({
  title,
  price,
  image,
  onPress,
}) => {
  return (
    <TouchableOpacity style={styles.card} onPress={onPress} activeOpacity={0.8}>
      <Image source={{ uri: image }} style={styles.image} />
      <View style={styles.info}>
        <Text style={styles.title} numberOfLines={2}>
          {title}
        </Text>
        <Text style={styles.price}>${price.toFixed(2)}</Text>
      </View>
    </TouchableOpacity>
  );
};

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    borderRadius: 12,
    overflow: 'hidden',
    marginBottom: 15,
    ...Platform.select({
      ios: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.1,
        shadowRadius: 4,
      },
      android: {
        elevation: 3,
      },
    }),
  },
  image: {
    width: '100%',
    height: 200,
    backgroundColor: '#f0f0f0',
  },
  info: {
    padding: 12,
  },
  title: {
    fontSize: 16,
    fontWeight: '600',
    color: '#333',
    marginBottom: 8,
  },
  price: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#007AFF',
  },
});

// Usage
const App = () => {
  return (
    <ScrollView style={{ flex: 1, padding: 15 }}>
      <ProductCard
        title="Wireless Headphones"
        price={99.99}
        image="https://example.com/headphones.jpg"
        onPress={() => console.log('Product pressed')}
      />
    </ScrollView>
  );
};
```

---

## Summary

In this module, you learned:

✅ Core React Native components and their native equivalents

✅ View, Text, Image, Button, and Pressable

✅ TextInput for user input with various keyboard types

✅ ScrollView for scrollable content

✅ Platform-specific code and styling

✅ Building reusable component (ProductCard)

**Next up**: Learn Flexbox layout and styling to build beautiful UIs!

<Card title="Next: Styling & Layout" icon="paint-brush" href="/courses/react-native-crash-course/03-styling-layout">
  Master Flexbox and create responsive designs
</Card>

---

## Interview Deep-Dive

<AccordionGroup>
  <Accordion title="Why does React Native require all text to be wrapped in a Text component, and what happens if you put a raw string inside a View?">
    **Strong Answer:**
    - If you write `<View>Hello</View>`, you get a runtime crash: "Text strings must be rendered within a Text component." This is not a React Native quirk -- it reflects the native platform reality. iOS and Android have distinct text rendering systems (UILabel/UITextView on iOS, TextView on Android) that handle font rendering, text layout, line breaking, and accessibility differently from generic containers.
    - React Native needs to know when content is text so it can route it to the correct native component. On the web, the browser handles this transparently because the DOM has inline and block contexts. Native platforms do not have this concept -- text and non-text are fundamentally different view types.
    - The practical implication extends to styling: Text components support text-specific properties (fontSize, fontWeight, lineHeight, textAlign) while View components do not. Nested Text components inherit parent text styles, mimicking CSS text inheritance but scoped to the Text tree only. This is the only place in React Native where style inheritance exists.

    **Follow-up:** You need to render a paragraph where one word is tappable and styled differently. How do you handle this without breaking the text flow?

    **Follow-up Answer:**
    - Use nested Text components. The outer Text defines the base paragraph style, and the inner Text for the tappable word overrides specific styles and adds an `onPress` handler. This maintains inline text flow because nested Text components render as inline elements within the parent text layout.
    - Example: `<Text style={styles.paragraph}>Read our <Text style={styles.link} onPress={handlePress}>privacy policy</Text> for details.</Text>`. The "privacy policy" text flows inline with the surrounding text, wrapping naturally, while being independently tappable and styled.
    - Do not use a Pressable inside a Text -- it breaks the text flow and creates a block-level element. Text's built-in `onPress` is the correct API for inline tappable text.
  </Accordion>

  <Accordion title="Explain the difference between ScrollView and FlatList. When would using ScrollView for a list be a performance mistake?">
    **Strong Answer:**
    - ScrollView renders all of its children immediately and keeps them all in memory. For a screen with 10-20 items, this is fine. FlatList implements virtualized rendering -- it only mounts the items visible on screen plus a configurable buffer, recycling item views as you scroll. This keeps memory usage constant regardless of list size.
    - Using ScrollView for a list of 500+ items is a performance catastrophe, especially on Android. All 500 items are rendered in a single synchronous pass, blocking the JS thread for potentially seconds. On a device with 2GB RAM, the combined memory of 500 complex card components can trigger the OS's low-memory killer, crashing your app.
    - FlatList also provides built-in features critical for production lists: pull-to-refresh, infinite scrolling via `onEndReached`, section headers via SectionList, item separators, empty state rendering, and scroll-to-index. Implementing these manually on ScrollView is error-prone.
    - The one scenario where ScrollView is correct for list-like content: when you have a small, fixed number of heterogeneous items (like a settings screen with 8 different row types) and want them to scroll together with non-list content above and below. Here, the virtualization overhead of FlatList outweighs its benefits.

    **Follow-up:** Your FlatList with 1000 items renders correctly but initial load takes 3 seconds before any content appears. Why, and how do you fix it?

    **Follow-up Answer:**
    - FlatList's `initialNumToRender` defaults to 10 items, but if your data fetching or state initialization is slow, the list waits for data before rendering anything. Use the `ListEmptyComponent` prop to show a skeleton loader while data loads.
    - If the data is already available but rendering is slow: the `initialNumToRender` items are rendered synchronously on first mount. If each item is complex (multiple images, nested components), rendering 10 of them synchronously can take 500ms+. Reduce `initialNumToRender` to 5, or simplify the item component.
    - Enable `getItemLayout` if items have fixed heights. Without it, FlatList must measure each item dynamically before determining scroll positions, which adds overhead. With `getItemLayout`, scroll position calculations are O(1) instead of O(n).
    - Consider FlashList as a drop-in replacement. It renders items faster by reusing native view instances (similar to Android's RecyclerView pattern) rather than unmounting and remounting React components.
  </Accordion>

  <Accordion title="The built-in Button component looks completely different on iOS versus Android. How do you build a cross-platform button that maintains platform-appropriate feel?">
    **Strong Answer:**
    - Use Pressable as the base, not TouchableOpacity or the built-in Button. Pressable gives you platform-specific feedback: on Android, use the `android_ripple` prop for Material Design ripple effects; on iOS, use the `style` callback to reduce opacity or apply a scale transform on press. This matches what users expect on each platform.
    - The key insight is that "cross-platform" does not mean "identical on both platforms." It means "feels native on each platform." Android users expect a ripple effect on tap. iOS users expect an opacity change or subtle scale animation. A button that looks and behaves identically on both platforms actually feels wrong to users of both.
    - Build a reusable Button component that accepts variant (primary, secondary, outline, ghost), size (sm, md, lg), loading state, and disabled state. Use `Platform.select` for platform-specific feedback, and `StyleSheet.create` for the static styles with dynamic style functions for pressed/disabled states.
    - For the loading state, replace the button text with an `ActivityIndicator` sized to match the text height, and set `disabled={true}` to prevent double-taps. This is a production pattern that prevents duplicate form submissions, which is especially important for payment or order flows.

    **Follow-up:** A designer wants the button to have identical appearance on both platforms, overriding platform conventions. How do you push back?

    **Follow-up Answer:**
    - Present data: Apple and Google both publish HIG and Material Design guidelines respectively. Users have muscle memory for platform conventions. A study by Google found that Material Design patterns on Android increase user confidence and reduce errors. Deviating from conventions creates cognitive friction.
    - However, there is a valid middle ground: brand-specific button styling (custom colors, typography, border radius) that still uses platform-appropriate feedback. The button can look "branded" while the interaction (ripple on Android, opacity on iOS) remains native. This is what apps like Instagram and Airbnb do -- their visual identity is consistent across platforms, but tap feedback follows platform conventions.
  </Accordion>
</AccordionGroup>
